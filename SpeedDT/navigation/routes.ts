export const routes = {
  onboarding: '/',
  consent: '/consent',
  tabs: {
    home: '/(tabs)/home',
    history: '/(tabs)/history',
    test: '/(tabs)/test',
    settings: '/(tabs)/settings',
  },
} as const;
